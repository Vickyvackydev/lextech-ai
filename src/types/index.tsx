export interface UserPayload {
  id: string;
  username: string;
  email: string;
}
export interface ProfilePayload {
  id: string;
  username: string;
  first_name: string;
  last_name: string;
  image: string;
  location: string;
  bio: string;
}
